'use client';

// Este archivo se mantiene por compatibilidad
// Todo el código se ha movido a AuthContextOptimized.jsx
// Re-exportamos desde allí para mantener compatibilidad con imports existentes

export * from './AuthContextOptimized';
export { useAuth, AuthProvider } from './AuthContextOptimized';
